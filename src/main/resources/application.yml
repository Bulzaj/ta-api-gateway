spring:
  application:
    name: ta-api-gateway
  cloud:
    gateway:
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: "*"
            allowedHeaders: "authorization"
            allowedMethods:
              - GET
              - POST

eureka:
  client:
    serviceUrl:
      defaultZone:
        http://localhost:5000/eureka/

server:
  port: 5100